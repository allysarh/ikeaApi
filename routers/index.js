const productsRouter = require('./productsRouter')
const userRouter = require('./usersRouters')
const transactionRouter = require('./transactionRouter')

module.exports = {
    userRouter, productsRouter, transactionRouter
}

